env: false # 是否开启环境变量
master:
  host:
  port: 30001
  password:
  maxIdle: 8
  maxActive: 0
  idleTimeout: 100

# slave配置
slave:
  enable: false
  host:
  port:
  password:
  maxIdle: 8
  maxActive: 0
  idleTimeout: 100

